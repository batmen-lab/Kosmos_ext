
# Outputs
*_output.txt
output.txt

# Env
.env
.env.backup

# Claude Code settings (keep commands/ directory)
.claude/settings.local.json
.claude/*.json

# Editors
.project
.settings
.springBeans
**/.favorites.json
**/.idea
**/.vscode
**/*.Session.vim
**/*.sw[a-z]
**/*.vim
**/*.zwc
**/*~

# OS generated files
**/._*
**/.AppleDouble
**/.dccache
**/.dropbox
**/.dropbox.attr
**/.dropbox.cache
**/.DS_Store
**/.DS_STORE
**/.lnk
**/.LSOverride
**/$RECYCLE.BIN/
**/Desktop.ini
**/ehthumbs.db
**/Thumbs.db

# Files that might appear in the root of a volume
**/.com.apple.timemachine.donotpresent
**/.DocumentRevisions-V100
**/.fseventsd
**/.Spotlight-V100
**/.TemporaryItems
**/.Trashes
**/.VolumeIcon.icns

# Directories potentially created on remote AFP share
**/.apdisk
**/.AppleDB
**/.AppleDesktop
**/Network Trash Folder
**/Temporary Items

# Temp files
**/.temp
**/.tmp
tmp/

# Files that commonly contain secrets
**/*.key
**/*.pem
**/*.pfx
**/*.p12
**/*.jks
**/*.keystore
**/*.pkcs12
**/*.pkcs8
**/*.pkpass
**/*.secrets

# Logs
**/*.log

# Build directories
**/_build/
**/dist
build/
out/
target/

# Python
__pycache__/
__pypackages__/
.ipynb_checkpoints
*$py.class
*.pyc
*.pyo
*.pyd
celerybeat-schedule
develop-eggs/
dmypy.json
eggs/
ipython_config.py
local_settings.py
pip-delete-this-directory.txt
pip-log.txt
pip-wheel-metadata/
venv
venv/
.venv/
env/
ENV/

# Coverage reports
.coverage
coverage.xml
htmlcov/

# Pytest
.pytest_cache/

# Local development databases
*.db
*.sqlite
*.sqlite3
.chroma_db/
.kosmos_cache/

# Development logs
logs/
*.jsonl
human_review_audit.jsonl

# Local data directories
postgres_data/
redis_data/
neo4j_data/
neo4j_logs/
neo4j_import/
neo4j_plugins/
pgadmin_data/

# Local analysis files
req_to_paper_analysis_*.md

# Screenshots (may contain sensitive info)
*.png
*.jpg
*.jpeg

# Literature cache
.literature_cache/
.literature_analysis_cache/
.concept_extraction_cache/

# Data files (large binary files excluded, benchmarks included)
data/*.h5ad
data/*.parquet
data/*.h5
# Donor-split CSVs used by the PPI / evidence experiments (hundreds of MB)
data/csv_donor_splits/
# Artifacts staged by the standalone datafetcher board (data + ledger)
data/fetched/
!data/benchmarks/
CLAUDE.md


# ---------------------------------------------------------------------------
# Kosmos_ext: local runs, staged data and vendored copies
# ---------------------------------------------------------------------------
# Every `run.py` question writes a plan, a data report, figures, checkpoints
# and pseudo-label tensors under artifacts/runs; a few weeks of testing is
# 2 GB, and all of it is reproducible from the command that made it. The small
# hand-written fixtures (`artifacts/baseline_run`, `artifacts/model_comparison`)
# stay tracked.
artifacts/runs/

# Dated archive folders kept locally while the ppi/flow modules were rewritten
# (`archived/ppi-signed-v1-2026-09-13`, `archived/removed-2026-09-14`).
archived/*-20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# ToolUniverse is vendored into the workspace as a clone with its own .git
# (163 MB). It is a dependency, not this repository's history: commit it as a
# submodule, or delete its .git, before tracking it here.
ToolUniverse/

# What a fetch stages, in its own formats. Keyed by reference and re-created by
# `datafetcher fetch`, so none of it belongs in git.
data/**/*.h5ad
data/**/*.h5ad.gz
data/**/*.csv.gz
data/**/*.mtx
data/**/*.mtx.gz
data/**/*.tar
data/**/*.bw

# Model checkpoints, pseudo-label tensors and matrices: always generated.
*.npz
*.pt
*.pth
*.ckpt
*.onnx

# Crash dumps: a segfault inside torch/BLAS leaves a multi-gigabyte core file.
core
core.*
*.core
vgcore.*

# Caches and state written by the tooling that runs in this workspace.
.ruff_cache/
.codex/
.agents/
*.egg-info/
